CMAKE_MINIMUM_REQUIRED(VERSION 3.18)
PROJECT("samples")

SET(SRC
    sample.cpp)

ADD_EXECUTABLE(samples "${SRC}")

TARGET_LINK_LIBRARIES(samples PRIVATE RogEng)
TARGET_INCLUDE_DIRECTORIES(samples PRIVATE ${CMAKE_SOURCE_DIRECTORY}/src/)
